def read_xlsx(path):
    unshaped_df = None
    return unshaped_df

def shape_df(df):
    shaped_df = None
    return shaped_df

def inject_db(df):
    def connect_db():
        return None
    def commit_db(df):
        return None
    def disconnect_db():
        return None
    connect_db()
    commit_db(df)
    disconnect_db
    return None


def main():
    xlsx_path = None
    read_xlsx(xlsx_path)
    shape_df(df)
    inject_db(df)